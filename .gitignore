# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Datos generados por la app (audio, transcripciones, crónicas)
datos/

# Craig self-hosted: se instala aparte, no forma parte de este repositorio.
# Contiene además los tokens de Discord en install.config.
app/craig/

# Configuración local del usuario (contiene los secretos de Discord)
config.ini

# Acceso directo: lleva rutas absolutas de esta máquina.
# Se genera con: python -m app.acceso_directo
Rolsumen.lnk
